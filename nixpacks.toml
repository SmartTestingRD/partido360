providers = ["node"]

[phases.setup]
  nixPkgs = ["nodejs_22"]

[phases.install]
  cmds = [
    "npm install --omit=dev",
    "npm install --prefix frontend"
  ]

[phases.build]
  cmds = ["VITE_API_URL=/api npm run build --prefix frontend"]

[start]
  cmd = "node src/server.js"
